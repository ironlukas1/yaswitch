# Problems — yaswitch-wayland-theme-manager

- Potential plan ambiguity: task references mention JSON/TOML in one place while locked contract specifies YAML/JSON.
- Some QA scenario steps in plan are descriptive rather than exact commands; convert to concrete commands during execution tasks.
- Need to keep task scope strict to avoid v1 scope creep (plugin systems, non-Wayland support, forced restarts).
